<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Linux on Qstudy的博客</title><link>http://dlgde.github.io/categories/linux/</link><description>Recent content in Linux on Qstudy的博客</description><generator>Hugo</generator><language>zh-CN</language><lastBuildDate>Mon, 18 Nov 2019 15:47:07 +0000</lastBuildDate><atom:link href="http://dlgde.github.io/categories/linux/index.xml" rel="self" type="application/rss+xml"/><item><title>linux系统进程状态有哪些？</title><link>http://dlgde.github.io/post/2019/linux%E7%B3%BB%E7%BB%9F%E8%BF%9B%E7%A8%8B%E7%8A%B6%E6%80%81%E6%9C%89%E5%93%AA%E4%BA%9B/</link><pubDate>Mon, 18 Nov 2019 15:47:07 +0000</pubDate><guid>http://dlgde.github.io/post/2019/linux%E7%B3%BB%E7%BB%9F%E8%BF%9B%E7%A8%8B%E7%8A%B6%E6%80%81%E6%9C%89%E5%93%AA%E4%BA%9B/</guid><description>&lt;h2 id="进程状态有哪些"&gt;进程状态有哪些？&lt;/h2&gt;
&lt;p&gt;在学校的操作系统原理中，我们系统中的进程有多种状态，那对应到常用的linux系统，有哪些进程状态呢？通过top可看到有以下的状态：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;R&lt;/strong&gt; 是 Running 或 Runnable 的缩写，表示进程在 CPU 的就绪队列中，正在运行或者正在等待运行。&lt;/p&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;D&lt;/strong&gt; 是 Disk Sleep 的缩写，也就是不可中断状态睡眠（Uninterruptible Sleep），一般表示进程正在跟硬件交互，并且交互过程不允许被其他进程或中断打断。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Z&lt;/strong&gt; 是 Zombie 的缩写，如果你玩过“植物大战僵尸”这款游戏，应该知道它的意思。它表示僵尸进程，也就是进程实际上已经结束了，但是父进程还没有回收它的资源（比如进程的描述符、PID 等）。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;S&lt;/strong&gt; 是 Interruptible Sleep 的缩写，也就是可中断状态睡眠，表示进程因为等待某个事件而被系统挂起。当进程等待的事件发生时，它会被唤醒并进入 R 状态。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;I&lt;/strong&gt; 是 Idle 的缩写，也就是空闲状态，用在不可中断睡眠的内核线程上。前面说了，硬件交互导致的不可中断进程用 D 表示，但对某些内核线程来说，它们有可能实际上并没有任何负载，用 Idle 正是为了区分这种情况。要注意，D 状态的进程会导致平均负载升高， I 状态的进程却不会。&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;除了上面的5种，还有&lt;strong&gt;T，S&lt;/strong&gt;，&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;T&lt;/strong&gt; 也就是 Stopped 或 Traced 的缩写，表示进程处于暂停或者跟踪状态。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;向一个进程发送 SIGSTOP 信号，它就会因响应这个信号变成暂停状态（Stopped）；再向它发送 SIGCONT 信号，进程又会恢复运行（如果进程是终端里直接启动的，则需要你用 fg 命令，恢复到前台运行）。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;X&lt;/strong&gt;，也就是 Dead 的缩写，表示进程已经消亡，所以你不会在 top 或者 ps 命令中看到它。&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id="关于僵尸进程"&gt;关于僵尸进程&lt;/h2&gt;
&lt;p&gt;问题：僵尸进程是怎么出现，有什么危害，应该怎么处理？&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;僵尸进程是因为父进程没有回收子进程的资源而出现的&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;正常情况下，当一个进程创建了子进程后，它应该通过系统调用 wait() 或者 waitpid() 等待子进程结束，回收子进程的资源；而子进程在结束时，会向它的父进程发送 SIGCHLD 信号，所以，父进程还可以注册 SIGCHLD 信号的处理函数，异步回收资源。如果父进程没这么做，或是子进程执行太快，父进程还没来得及处理子进程状态，子进程就已经提前退出，那这时的子进程就会变成僵尸进程。&lt;/p&gt;</description></item><item><title>理解平均负载和CPU使用率</title><link>http://dlgde.github.io/post/2019/%E7%90%86%E8%A7%A3%E5%B9%B3%E5%9D%87%E8%B4%9F%E8%BD%BD%E5%92%8Ccpu%E4%BD%BF%E7%94%A8%E7%8E%87/</link><pubDate>Sun, 17 Nov 2019 03:00:00 +0000</pubDate><guid>http://dlgde.github.io/post/2019/%E7%90%86%E8%A7%A3%E5%B9%B3%E5%9D%87%E8%B4%9F%E8%BD%BD%E5%92%8Ccpu%E4%BD%BF%E7%94%A8%E7%8E%87/</guid><description>&lt;h2 id="平均负载"&gt;平均负载&lt;/h2&gt;
&lt;p&gt;平均负载是指单位时间内，系统处于&lt;strong&gt;可运行状态&lt;/strong&gt;和&lt;strong&gt;不可中断状态&lt;/strong&gt;的平均进程数，也就是&lt;strong&gt;平均活跃进程数&lt;/strong&gt;，它和 CPU 使用率并没有直接关系。&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;使用uptime命令，依次是过去 1 分钟、5 分钟、15 分钟的平均负载（Load Average;&lt;/li&gt;
&lt;li&gt;理解平均负载， 比如当平均负载为 2 时，意味着什么呢？
&lt;ul&gt;
&lt;li&gt;在只有 2 个 CPU 的系统上，意味着所有的 CPU 都刚好被完全占用。&lt;/li&gt;
&lt;li&gt;在 4 个 CPU 的系统上，意味着 CPU 有 50% 的空闲。&lt;/li&gt;
&lt;li&gt;而在只有 1 个 CPU 的系统中，则意味着有一半的进程竞争不到 CPU。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;一般来说，&lt;strong&gt;当平均负载高于 CPU 数量 70% 的时候&lt;/strong&gt;，就应该分析排查负载高的问题;&lt;/p&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h2 id="平均负载和cpu使用率"&gt;平均负载和CPU使用率&lt;/h2&gt;
&lt;p&gt;平均负载是指单位时间内，处于可运行状态和不可中断状态的进程数。所以，它不仅包括了&lt;strong&gt;正在使用 CPU&lt;/strong&gt; 的进程，还包括&lt;strong&gt;等待 CPU&lt;/strong&gt; 和&lt;strong&gt;等待 I/O&lt;/strong&gt; 的进程。而 CPU 使用率，是单位时间内 CPU 繁忙情况的统计，跟平均负载并不一定完全对应。比如：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;CPU 密集型进程，使用大量 CPU 会导致平均负载升高，此时这两者是一致的；&lt;/li&gt;
&lt;li&gt;I/O 密集型进程，等待 I/O 也会导致平均负载升高，但 CPU 使用率不一定很高；&lt;/li&gt;
&lt;li&gt;大量等待 CPU 的进程调度也会导致平均负载升高，此时的 CPU 使用率也会比较高。&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id="工具使用"&gt;工具使用&lt;/h2&gt;
&lt;ol&gt;
&lt;li&gt;mpstat 是一个常用的多核 CPU 性能分析工具，用来实时查看每个 CPU 的性能指标，以及所有 CPU 的平均指标。
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;mpstat -P ALL 5 1&lt;/code&gt;,显示所有CPU的指标，并在间隔5秒输出一组数据&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;pidstat 是一个常用的进程性能分析工具，用来实时查看进程的 CPU、内存、I/O 以及上下文切换等性能指标。
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;pidstat -u 5 1&lt;/code&gt;,间隔5秒输出一组数据，-u 表示CPU指标&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ol&gt;</description></item><item><title>理解Linux中的buffer和cache</title><link>http://dlgde.github.io/post/2019/%E7%90%86%E8%A7%A3linux%E4%B8%AD%E7%9A%84buffer%E5%92%8Ccache/</link><pubDate>Sat, 16 Nov 2019 11:12:14 +0000</pubDate><guid>http://dlgde.github.io/post/2019/%E7%90%86%E8%A7%A3linux%E4%B8%AD%E7%9A%84buffer%E5%92%8Ccache/</guid><description>&lt;h2 id="磁盘和文件的区别"&gt;磁盘和文件的区别：&lt;/h2&gt;
&lt;p&gt;关于磁盘和文件的区别，本来以为大家都懂了，所以没有细讲。&lt;strong&gt;磁盘是一个块设备，可以划分为不同的分区；在分区之上再创建文件系统，挂载到某个目录，之后才可以在这个目录中读写文件&lt;/strong&gt;。&lt;/p&gt;
&lt;p&gt;其实 Linux 中“一切皆文件”，而文章中提到的“文件”是普通文件，磁盘是块设备文件，这些大家可以执行 &amp;ldquo;ls -l &amp;lt;路径&amp;gt;&amp;rdquo; 查看它们的区别（输出的含义如果不懂请 man ls 查询）。&lt;/p&gt;
&lt;p&gt;**在读写普通文件时，会经过文件系统，由文件系统负责与磁盘交互；而读写磁盘或者分区时，就会跳过文件系统，也就是所谓的“裸I/O“。**这两种读写方式所使用的缓存是不同的，也就是文中所讲的 Cache 和 Buffer 区别。&lt;/p&gt;
&lt;h2 id="buffer和cache的区别"&gt;buffer和cache的区别：&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;Buffer 是对磁盘数据的缓存，而 Cache 是文件数据的缓存，它们既会用在读请求中，也会用在写请求中&lt;/strong&gt;;&lt;/p&gt;
&lt;p&gt;插曲：理论上，一个文件读首先到Block Buffer, 然后到Page Cache。有了文件系统才有了Page Cache.&lt;br&gt;
在老的Linux上这两个Cache是分开的。那这样对于文件数据，会被Cache两次。这种方案虽然简单，&lt;br&gt;
但低效。后期Linux把这两个Cache统一了。对于文件，Page Cache指向Block Buffer，对于非文件&lt;br&gt;
则是Block Buffer。这样就如文件实验的结果，文件操作，只影响Page Cache，Raw操作，则只影响Buffer. 比如一此VM虚拟机，则会越过File System，只接操作 Disk, 常说的Direct IO.&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;free的数据来源，可以通过man free查看，来自/proc/meminfo;**/proc 是 Linux 内核提供的一种特殊文件系统，是用户 跟内核交互的接口。**比方说，用户可以从 /proc 中查询内核的运行状态和配置选项，查询进程的运行状态、统计数据等，当然，你也可以通过 /proc 来修改内核的配置。&lt;/p&gt;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;vmstat命令学习，vmstat 1表示每一分钟输出一组数据；bi 和 bo 则分别表示块设备读取和写入的大小，单位为块 / 秒。因为 Linux 中块的大小 是 1KB，所以这个单位也就等价于 KB/s。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;问题：“因为 Linux 中块的大小是 1KB，所以这个单位也就等价于 KB/s。”关于这一点，想请问下老师，linux block的大小不是4KB呢？&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;​ 回答：这句话来自vmstat的文档：https://linux.die.net/man/8/vmstat，通常说的的 Block Size 是磁盘分区的块大小， 的确都是 4KB 了&lt;/p&gt;</description></item></channel></rss>